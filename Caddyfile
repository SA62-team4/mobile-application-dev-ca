# HTTPS edge for the Spring backend.

{$API_DOMAIN} {
	reverse_proxy spring-backend:8080

	log {
		output stdout
		format console
	}

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		-Server
	}
}
