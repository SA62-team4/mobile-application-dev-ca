# Collections required by the deploy playbook. Installed in CI before the run:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.docker
    version: ">=3.10.0"
  - name: ansible.posix
    version: ">=1.5.0"
