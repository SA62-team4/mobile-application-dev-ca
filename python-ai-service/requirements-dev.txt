# Test-only dependencies for the Python AI service. Kept separate from
# requirements.txt so the production image stays lean.
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
