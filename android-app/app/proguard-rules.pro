# Release ProGuard rules for the wellness app.
# minifyEnabled is currently false, so these rules are inert; they are kept so a
# future release with shrinking enabled has a place to preserve the Retrofit/Gson
# model classes and Google Sign-In reflection paths.
