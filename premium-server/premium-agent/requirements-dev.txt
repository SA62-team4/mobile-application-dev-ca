-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
respx==0.21.1
# respx 0.21 patches httpx internals that changed in httpx 0.28, so 0.28 silently
# bypasses the mock ("... not mocked!"). Pin to the last 0.27 (still satisfies the
# runtime httpx>=0.27,<1). Bump this together with respx when upgrading either.
httpx==0.27.2
pydantic-settings