<Project>
  <PropertyGroup>
    <!-- Emit packages.lock.json so Semgrep Supply Chain (Managed Scans) can resolve deps without a build -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
  </PropertyGroup>
</Project>
